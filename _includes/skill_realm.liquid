<div class="skills-section">
  {% for skill in site.data.skill_realms %}
    <div class="skill-box">
      <i class="{{ skill.icon }} skill-icon"></i>
      <h3>{{ skill.title }}</h3>
      <p>{{ skill.description }}</p>
    </div>
  {% endfor %}
</div>
